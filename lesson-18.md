## Пакет strings

В повседневной работе программисту постоянно приходится иметь дело со строками — названиями товаров, адресами электронной почты, паролями, сообщениями, и многим другим текстом.

Примером задач, в которых может понадобиться пакет **`strings`**, служит сокращение полного имени пользователя с `Рогов Данил Николевич` до `Рогов Д. Н.»` или проверка пароля на наличие в нём цифр.

Для таких задач в Go существует пакет **`strings`** — один из самых популярных пакетов стандартной библиотеки.

Навык уверенно пользоваться этим пакетом — один из фундаментальных для любого разработчика на Go.
Зная его возможности, можно решать повседневные задачи буквально в несколько строк кода — просто и понятно.

### Наиболее популярные функции пакета

#### Смена регистра

```go
name := "Данил Рогов"

smallName := strings.ToLower(name)
bigName := strings.ToUpper(name)

fmt.Println(smallName) // "данил рогов"
fmt.Println(bigName) // "ДАНИЛ РОГОВ"
fmt.Println(name) // Данил Рогов
```

> ***Важно помнить, что строки в Go неизменяемы.
Когда мы вызываем функцию, которая, казалось бы, изменяет строку, на самом деле она возвращает новую строку — исходная при этом остаётся без изменений***. 

#### Проверка префикса

```go
number := "+7-800-123-21-21"

isRussianNumber := strings.HasPrefix(number, "+7")

fmt.Println(isRussianNumber) // true
```

#### Проверка суффикса

```go
email := "danil-rogov@yandex.ru"

isRussianEmail := strings.HasSuffix(email, ".ru")

fmt.Println(isRussianEmail) // true
```

#### Проверка, что строка содержит подстроку

```go
message := "Вы выиграли миллион! Чтобы получить, нажмите на ссылку https://virus.com" 

hasReference := strings.Contains(message, "https")

if (hasReference) {
    fmt.Println("Будьте осторожны! Сообщение содержит ссылку!")
}
```

#### Проверка, что строка содержит хотя бы один символ подстроки

```go
name := "Данил777"

containsNumbers := strings.ContainsAny(name, "01234567")

if (containsNumbers) {
    fmt.Println("Имя пользователя не должно содержать цифры!")
}
```

#### Подсчет сколько раз подстрока встречается в строке

```go
log := 
    "INFO start application; INFO: load settings; ERROR: database connection failed; ERROR: invalid settings"

errorCount := strings.Count(log, "ERROR")

fmt.Println("Количество ошибок:", errorCount) // 2
```

#### Объединение слайса/массива в строку

```go
things := []string{"Данил", "Go", "качалка"}

thingsInline := strings.Joint(things, " + ")

fmt.Println(thingsInline, "= машина") // Данил + Go + качалка = машина
```

#### Замена всех вхождений подстроки

```go
message := "Отличный урок! :) Я решил все задачи :)"

formattedText := strings.ReplaceAll(message, ":)", "🙂")

fmt.Println(formattedText)
```

Вывод:

```
Отличный урок! 🙂 Я решил все задачи 🙂
```

#### Разбиение строки на слайс подстрок 

```go
// Пользователь ввёл несколько телефонов в одной строке
input := "+7-999-111-22-33; +7-912-555-44-66; +7-495-000-11-22"

// Разделяем строку по символу ";" — получаем отдельные номера
phones := strings.Split(input, "; ")

fmt.Println("Найденные номера:")

// Выводим на экран все номера телефонов с новой строки
for _, phone := range phones {
	fmt.Println(phone)
}
```

Вывод:
```
Найденные номера:
+7-999-111-22-33
+7-912-555-44-66
+7-495-000-11-22
```

#### Удаление пробелов в начале и конце строки

```go
// Пользователь случайно ввел пробелы в начале и конце строки
input := " daniil-rogov@yandex.ru    "

// Удаляем лишние пробелы
cleanInput := strings.TrimSpace(input)

fmt.Println(cleanInput)
```

Вывод:
```
daniil-rogov@yandex.ru
```

#### Удаление подстрок из начала и конца строки

```go
// Ссылка на страницу в телеграмме 
link := "https://tg.com/daniil-777/"

fmt.Println(link)

// Удаляем подстроку из начала строки
link = strings.TrimPrefix(link, "https://tg.com/")

fmt.Println(link)

// Удаляем подстроку из конца строки
link = strings.TrimSuffix(link, "/")

// Остается только никнейм пользователя
fmt.Println(link)
```

Вывод:
```
https://tg.com/daniil-777/
daniil-777/
daniil-777
```

### Пример решения задачи

Пусть у нас есть письмо, полученное в виде строки.  
Из него нужно:
- Найти все телефонные номера и адреса электронной почты.
- Вывести аккуратный список контактов.

```go
// Исходный текст письма
letter := 
	"Здравствуйте! Меня зовут Данил Рогов, я представитель компании Ozon Tech! " +
	"Мои номера: +7-999-123-45-67 +1-202-555-0148 +44-20-7946-0958 " +
	"Моя почта: daniil.rogov@ozon.ru daniil.rgv@gmail.com"

// Разделяем строку по пробелам — получаем отдельные
lines := strings.Split(letter, " ")

// Создаём пустые слайсы, где будем хранить телефоны и email-адреса
phones := []string{}
emails := []string{}

// Проходим по каждому слову в тексте
for _, line := range lines {
    // Проверяем, начинается ли слово с телефонного кода
	isNumber := 
		strings.HasPrefix(line, "+7") || strings.HasPrefix(line, "+1") || strings.HasPrefix(line, "+44")

    // Если это номер телефона — добавляем его в слайс phones
	if (isNumber) {
		phones = append(phones, line)
	}

    // Проверяем, содержит ли слово символ '@' — значит, это e-mail
	isEmail := strings.Contains(line, "@")

    // Если это e-mail — добавляем в слайс emails
	if (isEmail) {
		emails = append(emails, line)
	}	
}

// Выводим все найденные телефоны
fmt.Println("Номера телефонов")

for _, phone := range phones {
	fmt.Println(phone)
}

// Выводим пустую строку
fmt.Println()

// Выводим все найденные почтовые ящики
fmt.Println("Почтовые ящики")

for _, email := range emails {
	fmt.Println(email)
}
```

Вывод: 
```
Номера телефонов
+7-999-123-45-67
+1-202-555-0148
+44-20-7946-0958

Почтовые ящики
daniil.rogov@ozon.ru
daniil.rgv@gmail.com
```

### Задачи для самостоятельного решения

####  1. Проверка надёжности пароля

Пользователь вводит пароль, а программа должна проверить, насколько он “хороший”.

Условия:

* Длина не меньше 8 символов
* Содержит хотя бы одну цифру
* Содержит хотя бы одну заглавную букву
* Не содержит пробелов

#### 2. Форматирование списка e-mail адресов

Дана строка:

```
"ivan@gmail.com; anna@mail.ru; petr@example.com; olga@yandex.ru"
```

Нужно:

- Разбить её на отдельные адреса
- Вывести только русские


#### 3. Форматирование телефонного номера

Пользователь вводит номер в любом виде:

```
"+7 999 123 45 67" или "+7-999-123-45-67"
```

Нужно убрать все пробелы и дефисы,
а затем вывести в едином формате:

```
+79991234567
```